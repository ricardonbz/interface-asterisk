{
  "compilerOptions": {
    "target": "ES6",                  // Define o padrão ECMAScript da saída (especifica a versão de JS que será gerada)
    "module": "commonjs",              // Define o sistema de módulos que o TypeScript irá usar (commonjs para Node.js)
    "outDir": "./dist",                // Pasta de saída onde os arquivos JS serão gerados
    "rootDir": "./src",                // Pasta de origem dos arquivos TypeScript
    "strict": true,                    // Ativa todas as opções de verificação estrita
    "esModuleInterop": true,           // Permite a importação de módulos CommonJS como módulos ES6
    "skipLibCheck": true,              // Ignora a verificação de tipo nas definições de tipo (.d.ts)
    "forceConsistentCasingInFileNames": true  // Garante que os nomes de arquivos sejam consistentes (importações maiúsculas/minúsculas)
  },
  "include": [
    "src/**/*.ts"                      // Incluir todos os arquivos TypeScript dentro da pasta 'src'
  ],
  "exclude": [
    "node_modules"                     // Excluir a pasta node_modules
  ]
}
